#include "logic.h"

Effects::Effects(){
	effect_list[0] = {"gain effect", gain_effect};
}

float32_t gain_effect(float32_t sample, void* params2) {
	float gain = *(float*)params;
    return sample * gain;
}
