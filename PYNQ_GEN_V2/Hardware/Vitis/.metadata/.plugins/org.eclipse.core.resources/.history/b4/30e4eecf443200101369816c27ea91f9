class signal
{
public:
	enum Type { SINE, SQUARE, SAW, TRIANGLE };
	signal(Type type, float frequency, float phase, float amplitude);

private:
	Type type;
	float frequency;
	float phase;
	float amplitude;
};
